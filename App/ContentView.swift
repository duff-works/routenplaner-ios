import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack(spacing: 12) {
            Text("Routenplaner iOS")
                .font(.largeTitle).bold()
            Text("Phase 0 ✓")
                .font(.title2)
                .foregroundStyle(.secondary)
        }
    }
}
